# KernSigner 项目总进展与详细计划

> 2026-05-22 智能卡更新：用户确认外接供电后 ACR39U-NF 读卡器可识别，Satochip/SeedKeeper 主线已经跑通。Satochip 已可测 Web3 连接/签名、路径地址和 BTC xpub/ypub/zpub/tpub/upub/vpub；SeedKeeper 已可测设置 PIN、改 PIN、写入助记词、查看/导入条目和重置。新版 SeedKeeper 重置必须用错 PIN/错 PUK 到 `FF00`，旧 `B0 FF` 返回 `9C20` 不是驱动坏。Satochip BTC PSBT/消息签名仍未作为生产能力开放。

本文档是当前项目的总控说明，面向 Waveshare ESP32-P4 WiFi6 Touch LCD 4.3 上的 KernSigner 验收工作。它回答三个问题：现在做到哪里、哪些边界不能作为已交付能力宣传、下一步怎么按安全顺序推进。

## 总结结论

当前项目已经完成“Krux 风格中文硬件底包 + 低风险工具 + 旧 Kern 钱包核心接入 + 智能卡测试主线 + 自动交付验收”的测试资金候选闭环。最新固件版本是 `0.0.7-rc1`，目标板是 Waveshare ESP32-P4 WiFi6 Touch LCD 4.3。仓库已包含 `firmware/wave_43/` 下的 untested full/app-only 固件；`_release/LATEST_RELEASE.txt` 只指向上一次脚本生成的交付包，不一定等同于当前源码或预置固件。

2026-05-24 最新预置固件哈希：app-only `bd50d526089b13d7af360e0ef4514b5e961564138452bb2c5a028f6132dac502`，full image `c31bb74caed08a17313ae0693c2b3a17e09cb3e0f9a2ae7cb7616df3622a282a`。这版已经本地构建、通过模拟器验收、通过 OKX 图片/视频桌面解码样本，并在 `/dev/ttyACM0` 上完成 app-only 真机刷写和启动日志检查。

当前版本可以用于验收：

- 中文主界面、Krux 功能目录、4.3 寸屏幕排版。
- 屏幕、触摸、背光和版本信息显示。
- 相机安全预览和普通二维码识别。
- 文本生成二维码。
- 普通文本/URL/公开 URI 的二维码分类显示。
- UR、BBQR、PSBT、SeedQR、二进制等高风险二维码识别后拦截。
- 存储卡读写测试和只读文件浏览。
- 旧 Kern 钱包核心和真机钱包入口：加载助记词、新助记词、钱包首页、扩展公钥、地址、备份、扫码签名真实流程。
- Web3 观察钱包连接码：当前钱包 EVM 地址二维码和观察账户连接能力。
- 钱包核心单测：派生、白名单、注册表、PSBT 分类等核心逻辑已通过。
- 自动截图、中文缺字检查、UI 烟测、滚动页截图、按钮导航验收。
- 一键打包、一键最终校验和可选一键刷机交付。

当前版本不能直接用于生产真钱：

- 未经过真机全流程验收的主网真实助记词、私钥、扩展公钥和钱包描述符。
- 未经过独立交叉校验的地址派生、备份、擦除和会话清理。
- 未经过交易详情审查、拒签、取消和错误路径测试的 PSBT/消息签名。
- 未完成安全审查的 SeedQR、加密备份、BIP85 和高级助记词工具。
- 未完成的 Satochip BTC 卡片签名操作，以及未经过生产审计的智能卡维护操作。
- 未完成 EVM 交易可读解析、Secure Channel 认证加固和生产配置前，Satochip Web3 签名只作为测试资金验收能力。
- 任何会导致真实资产转移或泄露的钱包动作。

这不是拖延，而是生产审计边界。当前是真钱包功能版，但不是生产资金审计版；钱包类功能必须在测试资金演练、真机交互确认、安全审查、失败回滚方案都具备后，才允许真实资金使用。

## 最新交付状态

最新交付包由以下文件指向：

```bash
cat /home/ak/123/Kern/_release/LATEST_RELEASE.txt
```

注意：这个指针不会因为只改代码或只改文档自动更新。只有用户明确确认“出固件”后，重新执行打包流程，才会生成新的交付包并更新该指针。

一键最终校验：

```bash
cd /home/ak/123/Kern
tools/signer_delivery.sh final
```

重新生成交付包并最终校验：

```bash
cd /home/ak/123/Kern
JOBS=2 tools/signer_delivery.sh ship
```

刷写真机、抓启动日志、重新打包并最终校验：

```bash
cd /home/ak/123/Kern
JOBS=2 ESPPORT=/dev/ttyACM0 ESPBAUD=115200 tools/signer_delivery.sh shipflash
```

## 当前自动验收结果

最新真钱包功能版已通过以下验收：

- 页面清单：最新交付验收批次为 151 个页面，详见 `docs/screens/delivery_20260523_175620/manifest.tsv`。
- 页面截图：最新交付验收批次共 299 张 BMP 截图，并生成关键页 PNG 与拼图。
- BMP 截图：本地重复导出文件；GitHub 预览以 PNG 为准。
- 截图失败：0。
- 中文/UI 缺字：0。
- UI 烟测失败：0。
- 滚动页截图失败：0。
- 按钮导航验收：0 个失败。
- 首页标题：`Home`。
- 真机启动日志：`docs/logs/boot_20260523_182345.log` 通过，包含 app 版本、屏幕初始化、GT911 触摸、LVGL task 和背光初始化。
- 模拟器交付验收状态：`FINAL: PASS`。
- OKX 桌面样本：静态图 + 1 fps 视频抽帧 `12/12 decoded`；全帧压力抽样 `256/257 decoded`，唯一失败帧是过渡/模糊帧。

## 代码结构进展

### 启动路径

`main/main.c` 已经从旧钱包登录流程改为直接进入 Krux 迁移 Shell。启动流程现在是：

- 初始化 NVS 和设置。
- 启动 Waveshare 4.3 寸显示和触摸。
- 清空旧 framebuffer，避免热重启残影。
- 设置背光亮度。
- 进入 `signer_shell_create()`。

### UI 与功能目录

主要代码在：

- `main/pages/signer_shell/signer_shell.c`
- `main/pages/signer_shell/signer_shell.h`
- `main/signer_port/signer_feature_catalog.c`
- `main/signer_port/signer_services.c`

当前 UI 是中文 Krux 功能目录，同时增加了旧 Kern 钱包真实入口。每个功能都有迁移状态、风险等级、来源 Krux 路径和下一步提示；真机固件中的 `legacy_*` 入口会跳到旧 Kern 钱包页面，模拟器为稳定验收映射到对应 Krux Shell 页面。

### 硬件与低风险工具

主要代码在：

- `main/signer_port/signer_hardware_probe.c`
- `main/signer_port/signer_camera_preview.c`
- `main/signer_port/signer_qr_decoder.c`
- `main/signer_port/signer_storage_browser.c`
- `main/core/settings.c`

当前已经接入：

- 显示和触摸。
- 背光亮度设置。
- 相机安全预览。
- 普通二维码识别和风险分类。
- 文本生成二维码。
- 存储卡读写自测。
- 存储卡只读浏览。
- 硬件快照。

### 自动验收与交付工具

主要代码在：

- `simulator/src/main_sim.c`
- `tools/signer_delivery.sh`
- `tools/bake_signer_cn_fonts.py`

当前已经接入：

- 中文字体子集自动烘焙。
- 模拟器构建。
- 77 页全量截图。
- 可滚动页面底部截图。
- 中文缺字检查。
- UI 标签/可点击对象烟测。
- 按钮导航自动点击验收。
- 关键页面 PNG。
- 全页面拼图。
- 交付包归档。
- SHA256SUMS 校验。
- Linux/Windows 刷机脚本刷前校验 `kernsigner.bin` SHA256。
- 真机启动日志自动判定。
- `ship` 一键交付。
- `shipflash` 一键刷机交付。
- `final` 一键最终校验。

## 功能迁移统计

总功能项：77。

按迁移状态统计：

- 已验证：9。
- 硬件已接：43。
- 界面就绪：7。
- 待接服务：4。
- 未开始：14。

按风险等级统计：

- 公开查看：18。
- 秘密材料：32。
- 签名操作：10。
- 外部输入输出：9。
- 设备控制：8。

服务状态：

- 可用：钱包服务、加密服务、二维码服务、显示触摸、相机服务、存储服务。
- 未接专项：BIP85 扩展、打印机、智能卡写卡/改 PIN/重置、SeedKeeper 管理、Satochip BTC PSBT/消息签名等不放进真钱包主菜单。Satochip Web3 连接/签名已进入可测主线。
- 智能卡服务：安全检测已接入，钱包级能力继续分阶段保护。

## 已完成清单

### 交付底包

- ESP32-P4 4.3 寸屏幕启动链路已跑通。
- GT911 触摸识别已跑通。
- 背光设置已跑通。
- `0.0.7-rc1` 固件可构建、可刷写、可启动。
- 交付包可重复生成。
- 最新包内包含固件、验收报告、启动日志、截图、拼图、SHA256、快速说明、刷前校验脚本、刷机命令和文件索引。

### 中文 UI

- 当前首页六入口为“扫码签名、加载助记词、创建助记词、连接钱包、设置、设备检查”。
- 连接钱包入口已收敛为“Web3钱包、比特币钱包、自定义路径”；比特币钱包再选择“已加载助记词、智能卡账户”。
- 解锁后钱包菜单、扫码签名、助记词、连接钱包和设备检查入口已按当前 Kern/Krux Shell 结构整理为中文菜单。
- 4.3 寸 480x800 布局已适配。
- 字体子集包含当前界面所需中文。
- 自动缺字检查为 0。
- 首页不再展示旧交付卡片，改为本地 Krux 风格的黑底、白状态条、居中白色列表。

### 低风险功能

- 普通文本二维码生成可用。
- 普通二维码扫描分类可用。
- 高风险二维码只分类和拦截。
- 存储卡读写测试可用。
- 存储卡根目录只读浏览可用。
- 亮度调节可用。
- 相机参数保存入口可用。
- 触摸测试入口可用。

### 交付自动化

- `tools/signer_delivery.sh check`：非刷机验收。
- `tools/signer_delivery.sh appflash`：app-only 刷机和启动日志。
- `tools/signer_delivery.sh release`：打包。
- `tools/signer_delivery.sh ship`：打包并最终校验。
- `tools/signer_delivery.sh shipflash`：刷机、启动日志、打包并最终校验。
- `tools/signer_delivery.sh final`：校验最新交付包。

## 未完成但不能跳过的风险项

### USB CCID 智能卡

当前状态：安全检测已接入，可作为读卡器/ATR/状态验收；Satochip Web3 连接/常见 EVM 签名、路径地址和 BTC 观察公钥读取已进入可测主线；SeedKeeper 设置 PIN、改 PIN、写入助记词、查看/导入条目和新版重置流程已进入测试卡验收范围。TypedData 和 Satochip BTC PSBT/消息签名仍未作为生产能力开放。

原因：

- ACR39U-NF Pocketmate II 是 CCID 读卡器，软件需要 USB Host CCID 驱动和 APDU 层。
- 之前真机直插读卡器供电和枚举不稳定；现在确认需要外接供电 Hub 或供电转接线。
- 钱包智能卡功能涉及密钥材料，当前只把已经跑通的 Satochip 读公钥/地址/Web3 摘要签名和 SeedKeeper 测试卡维护作为验收能力；生产资金版仍需要协议、安全和 UI 审查。

后续最低验收标准：

- USB Host 能稳定枚举设备。
- 能读取 VID/PID、接口类、端点描述符。
- 能在外接供电下稳定上电读卡器。
- 能读取卡 ATR。
- 能发送只读 APDU 并拿到稳定响应。
- 能识别 Satochip/SeedKeeper AID 并读取状态。
- Satochip Web3 签名必须只用测试资金回归，确认二维码、PIN、签名、返回码和二次签名后的相机恢复都稳定。
- SeedKeeper 写入、查看、导入、改 PIN、重置必须只用测试卡回归，确认不会蓝屏、不会误清卡。
- 不能暴露 TypedData 或 BTC 卡片签名假入口。

### 钱包/助记词/签名

当前状态：旧 Kern 钱包核心和真机入口已接入，真钱包主路径走旧 Kern 真实页面；未接的 Krux 专项功能不放进主菜单。

原因：

- 当前已把旧 Kern 的助记词加载/创建、钱包首页、扩展公钥、地址、备份、扫码签名入口编译进 ESP32-P4 真机固件。
- 核心单测已通过，但生产资金审计版还需要安全会话、PIN、内存清理、错误处理、备份保护、交易审查和拒签路径的真机回归。
- 如果把功能版直接当生产资金审计版，会制造“能用但未审计”的假完成状态。

后续最低验收标准：

- 先使用测试助记词、测试资金和测试 PSBT。
- 不允许未经审查的默认主网真签名。
- 每个敏感动作都有二次确认。
- 每个泄密路径都有拦截。
- PSBT 必须完整显示输入、输出、手续费、找零判断。
- 错误和取消路径必须可恢复。

## 全面详细计划

### 阶段 0：硬件底包和交付管线

状态：已完成。

目标：

- 开发板能稳定启动。
- 屏幕、触摸、背光可用。
- 中文 UI 可显示。
- 自动构建、截图、验收、打包、刷机日志形成闭环。

已完成：

- Waveshare 4.3 寸目标板适配。
- Krux 中文迁移 Shell。
- 中文字体烘焙。
- 模拟器截图验收。
- 一键交付脚本。
- 真机启动日志判定。

验收标准：

- `tools/signer_delivery.sh final` PASS。
- `FINAL_READINESS.txt` 为 PASS。
- 启动日志包含版本、显示、GT911、背光。

### 阶段 1：项目总控和可交付文档

状态：本轮完成。

目标：

- 项目进展不再靠聊天记录，而是落到仓库和交付包。
- 任何人打开交付包都知道先看什么、怎么刷机、怎么验收。

已完成：

- `PROJECT_PROGRESS_AND_PLAN.md` 记录全局进展和详细计划。
- `README_FIRST.txt` 作为交付包快速入口。
- `FLASH_COMMANDS.txt` 提供刷机和校验命令。
- `RELEASE_INDEX.tsv` 作为交付包文件索引。
- `ship` 和 `shipflash` 作为一键工作流。

验收标准：

- 最新 release 包包含上述文件。
- `tools/signer_delivery.sh final` 校验这些文件存在。

### 阶段 2：真机人工验收

状态：等待下一轮实机点测。

目标：

- 用户拿着开发板实际点击每个已开放低风险功能。
- 验证模拟器通过的界面在真机触摸上也舒服。

执行顺序：

- 开机确认首页标题。
- 进入交付验收页确认版本和硬件快照。
- 测试触摸坐标。
- 调整亮度。
- 打开相机预览。
- 扫普通文本/URL 二维码。
- 生成文本二维码。
- 插 FAT32 存储卡做读写测试。
- 刷新存储卡文件列表。

验收标准：

- 无 panic、assert、watchdog、brownout。
- 无明显乱码和方框。
- 页面能返回。
- 相机预览退出后可再次进入。
- 存储卡测试写读删成功。

### 阶段 3：USB CCID 专项

状态：安全检测已实现，继续做真机稳定性验证。

目标：

- 不碰钱包密钥，只证明 ESP32-P4 能稳定作为 USB Host 连接 ACR39U-NF，并读取 ATR/只读状态。

计划：

- 保留独立 CCID probe 作为排障固件。
- Kern 内置 `设备检查 -> 智能卡检测`。
- 继续热插拔、外接供电、Hub、卡片插拔回归。
- Satochip 路径地址和 BTC 观察公钥已接入；SeedKeeper 设置 PIN、改 PIN、写入助记词、查看/导入条目和新版重置流程已进入测试卡验收范围，但仍不能宣传为生产审计能力。

退出条件：

- 如果供电或枚举无法稳定，只保留外接供电方案，不把直插标记为可用。
- 不允许把检测页通过等同于智能卡钱包签名可用。

### 阶段 4：只读钱包能力

状态：计划中。

目标：

- 已接入旧 Kern 只读钱包入口，继续用测试助记词和公开数据做交叉校验，不把结果当生产资金审计版。

计划：

- 会话状态和测试助记词链路。
- 公钥/描述符展示并用独立工具交叉校验。
- 地址派生展示并用独立工具交叉校验。
- 地址二维码展示。
- 存储卡导出公开描述符。

安全要求：

- 默认不接受真实助记词。
- 所有导出明确标记测试/假数据。
- 所有页面可取消和返回。

### 阶段 5：真实钱包前置安全

状态：进行中，必须在生产资金使用前完成。

目标：

- 建立能承受真实资产风险的基础安全层。

计划：

- PIN/会话锁定。
- 内存清理。
- 真实助记词输入安全键盘。
- SeedQR/文件导入二次确认。
- 敏感信息显示倒计时和遮挡策略。
- 错误路径和取消路径。
- 自动测试未接专项页面和真钱包主路径。

验收标准：

- 不能因误触显示助记词。
- 不能因扫码自动导入种子。
- 不能因文件存在自动读取钱包秘密。
- 断电/重启后敏感会话不残留。

### 阶段 6：PSBT 和签名

状态：已接入旧 Kern 扫码签名，生产资金签名前必须完成真机安全验收。

目标：

- 只用测试 PSBT 验收扫码、解析、审查、拒签、取消和导出；在完整审查和拒签能力具备后，才允许真实资金签名。

计划：

- PSBT 解析。
- 输入/输出/手续费显示。
- 找零识别。
- 地址归属判断。
- 危险交易提示。
- 二次确认。
- 签名导出。

验收标准：

- 无法完整审查的 PSBT 必须拒签。
- 不能隐藏手续费和输出地址。
- 不能默认允许主网签名。
- 必须支持取消并清理临时数据。

### 阶段 7：生产安全

状态：计划中。

目标：

- 从开发功能版走向可交付生产固件。

计划：

- Secure Boot 策略。
- Flash Encryption 策略。
- Anti-rollback 策略。
- 固件签名和发布密钥管理。
- 出厂初始化流程。
- 恢复和救砖流程。
- 发布版本号和安全版本策略。

验收标准：

- 生产固件和开发固件明确区分。
- 不可逆安全开关必须有书面确认。
- 发布包可复现。

## 当前优先级

1. 保持 `ship` 和 `final` PASS。
2. 做真机人工验收记录。
3. 如果继续读卡器，单独做 USB CCID probe，不碰钱包。
4. 如果继续钱包，先用测试助记词/测试资金把创建、导入、xpub、地址、备份、擦除、PSBT 和消息签名跑完。
5. 所有新增中文必须跑字体烘焙和缺字检查。
6. 每次交付都必须生成 release 包并通过最终校验。

## 交付完成定义

本阶段交付完成的定义是：

- 最新交付包存在。
- `README_FIRST.txt` 可指导用户打开包。
- `FLASH_COMMANDS.txt` 可指导刷机。
- `FINAL_READINESS.txt` 显示 PASS。
- `ACCEPTANCE_REPORT.txt` 显示 FINAL PASS。
- `tools/signer_delivery.sh final` 显示 PASS。
- 真机启动日志显示版本、屏幕、触摸、背光。
- 生产审计边界清晰，已接入真钱包能力，但未批准生产资金使用。

当前已经满足本阶段交付完成定义。
